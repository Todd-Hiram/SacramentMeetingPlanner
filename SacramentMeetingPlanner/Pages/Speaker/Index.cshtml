@page
@model SacramentMeetingPlanner.Pages.Speaker.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Speaker[0].FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Speaker[0].LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Speaker[0].Topic)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Speaker[0].Order)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Speaker[0].Planner)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Speaker) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Topic)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Order)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Planner.BishopricMemeber)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.SpeakerId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.SpeakerId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.SpeakerId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
